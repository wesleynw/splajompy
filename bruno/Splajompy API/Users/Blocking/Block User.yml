info:
  name: Block User
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiUrl}}/user/:userId/block"
  params:
    - name: userId
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
