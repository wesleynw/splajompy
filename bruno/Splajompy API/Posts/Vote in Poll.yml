info:
  name: Vote in Poll
  type: http
  seq: 3

http:
  method: POST
  url: "{{apiUrl}}/post/:postId/vote/:optionIndex"
  params:
    - name: postId
      value: "674"
      type: path
    - name: optionIndex
      value: "0"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
