info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: "{{apiUrl}}/register"
  body:
    type: json
    data: |-
      {
          "username": "wesleynw",
          "email": "wesleynw@pm.me",
          "password": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
