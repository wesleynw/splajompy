info:
  name: Get Unread Notifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/notifications/unread/time"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
