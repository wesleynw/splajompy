info:
  name: Get Push Preferences
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiUrl}}/user/push-preferences"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
